# 3X-UI + Certbot (Cloudflare DNS)

Dockerized [3X-UI](https://github.com/MHSanaei/3x-ui) panel with automatic TLS-wildcard via Cloudflare DNS-01 challenge.

## Architecture
???

## Quick start

```bash
git clone <repo-url> && cd xui

cp .env.example .env   ## Create .env in the project root

docker compose up -d
